<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Congratulations! Onboarding Complete - {{companyName}}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #2E7D32; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background: #f9f9f9; }
        .button { background: #2E7D32; color: white; padding: 12px 24px; text-decoration: none; border-radius: 4px; display: inline-block; margin: 20px 0; }
        .success { background: #d4edda; border: 1px solid #c3e6cb; color: #155724; padding: 15px; border-radius: 4px; margin: 15px 0; }
        .footer { font-size: 12px; color: #666; text-align: center; padding: 20px; border-top: 1px solid #ddd; }
        .compliance { font-size: 11px; color: #888; margin-top: 20px; }
        .next-steps { background: #fff; border-left: 4px solid #2E7D32; padding: 15px; margin: 15px 0; }
        .celebration { font-size: 48px; text-align: center; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🎉 Onboarding Complete!</h1>
        </div>
        
        <div class="content">
            <div class="celebration">🎆✨🎆</div>
            
            <h2>Congratulations {{clientName}}!</h2>
            
            <div class="success">
                <strong>✓ Your onboarding process has been successfully completed!</strong>
            </div>
            
            <p><strong>Client ID:</strong> {{clientId}}</p>
            <p><strong>Completion Date:</strong> {{completionDate}}</p>
            <p><strong>Processing Time:</strong> {{processingDays}} days</p>
            
            <p>We're excited to welcome you as a fully onboarded member of the {{companyName}} community!</p>
            
            <div class="next-steps">
                <h3>🚀 What's Next?</h3>
                <ul>
                    {{#if nextSteps}}
                        {{#each nextSteps}}
                            <li>{{this}}</li>
                        {{/each}}
                    {{else}}
                        <li>Access your full account features</li>
                        <li>Explore our services and resources</li>
                        <li>Connect with your dedicated account manager</li>
                        <li>Set up any additional preferences</li>
                    {{/if}}
                </ul>
            </div>
            
            {{#if accountManager}}
            <p><strong>Your Account Manager:</strong> {{accountManager.name}}<br>
            <strong>Email:</strong> {{accountManager.email}}<br>
            <strong>Phone:</strong> {{accountManager.phone}}</p>
            {{/if}}
            
            <p>If you have any questions or need assistance getting started, our support team is here to help!</p>
            
            <a href="{{portalUrl}}" class="button">Access Your Full Account</a>
            
            <p>Thank you for choosing {{companyName}}. We look forward to serving you!</p>
            
            <p>Best regards,<br>The {{companyName}} Team</p>
        </div>
        
        <div class="footer">
            <div class="compliance">
                <p>This completion notification was sent to {{clientEmail}} regarding your successful onboarding.</p>
                <p>If you no longer wish to receive these emails, you can <a href="{{unsubscribeUrl}}">unsubscribe here</a>.</p>
                <p>{{companyName}} • {{companyAddress}} • Privacy Policy: {{privacyPolicyUrl}}</p>
                <p>© {{currentYear}} {{companyName}}. All rights reserved.</p>
            </div>
        </div>
    </div>
</body>
</html>